---
/**
 * ParticleBackground.astro
 * True N-Body Dual Galaxy Merger.
 * Features: Dynamical Friction, Inelastic Core Mergers, and Softened Gravity.
 */
---

<canvas
  id="galaxy-canvas"
  class="fixed inset-0 z-0 pointer-events-none"
  aria-hidden="true"></canvas>

<script>
  import * as THREE from "three";
  import { GPUComputationRenderer } from "three/examples/jsm/misc/GPUComputationRenderer.js";

  declare global {
    interface Window {
      appStats?: { begin: () => void; end: () => void };
    }
  }

  // Persistent state. Detect screen size: if mobile (<768px), default to 32, otherwise 72.
  let currentTextureWidth = window.innerWidth < 768 ? 32 : 72;
  let currentGalaxyRadius = 35;
  let currentOffset = 25;
  let isPaused = false;

  const init = () => {
    const canvas = document.getElementById(
      "galaxy-canvas"
    ) as HTMLCanvasElement;
    if (!canvas) return;

    // ============================================================================
    // CONFIGURATION ZONE
    // ============================================================================

    // --- Physics Engine (The Laws of the Universe) ---
    // TIME_STEP: How much time passes per frame.
    const TARGET_FPS = 60;
    const TIME_STEP = 0.08;

    // GRAVITY: Universal gravitational constant. IRL it is 6.674 x 10^-11.
    // Here it is 0.0001 because the mass and distance units are arbitrary.
    const GRAVITY = 0.0001;

    // MAX_SPEED: The "Speed of Light". Prevents stars from breaking the
    // physics engine if they get perfectly slingshotted by a black hole.
    const MAX_SPEED = 10.0;

    // SOFTENING: Gravitational softening is a numerical trick introduced in particle
    // simulations to avoid too close encounters that would otherwise result in
    // unrealistic motion of particles.
    const SOFTENING = 3.5;

    // FRICTION (Dynamical Friction): Only applies to the massive black holes.
    // It simulates the drag they experience when wading through the star field,
    // pulling them into an inevitable collision.
    const FRICTION = 0.998;

    // --- Galaxy Morphology ---
    const CORE_MASS = 4000000; // Mass of the Supermassive Black Holes
    //const GALAXY_RADIUS = 35; // Radius of the galaxies
    //const OFFSET = 25; // Spawn distance from the center
    const MERGE_FACTOR = 0.98; // Forces an elliptical orbit that degrades into a crash
    const MERGE_RADIUS = 4.0; // The Event Horizon. If BHs get this close, they fuse.

    // --- Visuals ---
    const PARTICLE_BASE_SIZE = 150.0;
    const BLACK_HOLE_SIZE = 10.0;
    const GALAXY_1_COLORS = ["#fb923c", "#facc15", "#ef4444"];
    const GALAXY_2_COLORS = ["#38bdf8", "#818cf8", "#e879f9"];

    // ============================================================================
    // 1. SCENE SETUP (Done ONLY ONCE to prevent WebGL context crashes)
    // ============================================================================
    const renderer = new THREE.WebGLRenderer({
      canvas,
      alpha: true,
      antialias: true,
      powerPreference: "high-performance",
    });
    renderer.setSize(window.innerWidth, window.innerHeight);
    renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));

    const scene = new THREE.Scene();
    const camera = new THREE.PerspectiveCamera(
      60,
      window.innerWidth / window.innerHeight,
      0.1,
      800
    );
    camera.position.set(0, 90, 160);
    camera.lookAt(0, 0, 0);

    // Dynamic variables that will be recreated when the slider is moved
    let gpuCompute: any;
    let positionVariable: any;
    let velocityVariable: any;
    let geometry: THREE.BufferGeometry | null = null;
    let material: THREE.ShaderMaterial | null = null;
    let points: THREE.Points | null = null;

    // ============================================================================
    // 2. GALAXY BUILDER (Runs initially, and every time the slider moves)
    // ============================================================================
    const buildGalaxy = (width: number) => {
      // Safely dispose of the old galaxy data if it exists
      if (geometry) geometry.dispose();
      if (material) material.dispose();
      if (points) scene.remove(points);

      const count = width * width;
      gpuCompute = new GPUComputationRenderer(width, width, renderer);
      if (renderer.capabilities.isWebGL2 === false) {
        gpuCompute.setDataType(THREE.HalfFloatType);
      }

      const dtPosition = gpuCompute.createTexture();
      const dtVelocity = gpuCompute.createTexture();
      const posData = dtPosition.image.data as Float32Array;
      const velData = dtVelocity.image.data as Float32Array;

      const binaryVelocity = Math.sqrt(
        (GRAVITY * CORE_MASS) / (2 * currentOffset)
      );
      const sysVz = binaryVelocity * MERGE_FACTOR;

      const bh1_index = 0;
      const bh2_index = Math.floor(count / 2);

      const bh1_uv = new THREE.Vector2(
        (bh1_index % width) / width + 0.5 / width,
        Math.floor(bh1_index / width) / width + 0.5 / width
      );
      const bh2_uv = new THREE.Vector2(
        (bh2_index % width) / width + 0.5 / width,
        Math.floor(bh2_index / width) / width + 0.5 / width
      );

      for (let i = 0; i < count; i++) {
        const p = i * 4;
        const isGalaxy1 = i < count / 2;

        const cx = isGalaxy1 ? -currentOffset : currentOffset;
        const cz = isGalaxy1 ? -10 : 10;
        const gVx = 0;
        const gVz = isGalaxy1 ? sysVz : -sysVz;

        if (i === bh1_index || i === bh2_index) {
          posData[p + 0] = cx;
          posData[p + 1] = 0;
          posData[p + 2] = cz;
          posData[p + 3] = CORE_MASS;
          velData[p + 0] = gVx;
          velData[p + 1] = 0;
          velData[p + 2] = gVz;
          velData[p + 3] = 0.0;
          continue;
        }

        const r = Math.pow(Math.random(), 2.0) * currentGalaxyRadius + 4.0;
        const theta = Math.random() * Math.PI * 2;
        const localX = Math.cos(theta) * r;
        const localZ = Math.sin(theta) * r;
        const localY = (Math.random() - 0.5) * (currentGalaxyRadius - r) * 0.12;

        posData[p + 0] = cx + localX;
        posData[p + 1] = localY;
        posData[p + 2] = cz + localZ;
        posData[p + 3] = Math.random() * 0.5 + 0.1;

        const softeningSq = SOFTENING * SOFTENING;
        const adjustedGravity =
          (GRAVITY * CORE_MASS * r * r) / Math.pow(r * r + softeningSq, 1.5);
        const v_mag = Math.sqrt(adjustedGravity);

        velData[p + 0] = gVx + (-localZ / r) * v_mag;
        velData[p + 1] = (Math.random() - 0.5) * 0.05;
        velData[p + 2] = gVz + (localX / r) * v_mag;
        velData[p + 3] = 0.0;
      }

      // --- SHADERS ---
      const computeVelocityShader = `
        uniform float uTimeStep;
        uniform float uSofteningSq;
        uniform float uG;
        uniform float uFriction;
        uniform float uMaxSpeed;
        
        uniform vec2 uBH1;
        uniform vec2 uBH2;
        uniform float uMergeRadius;

        void main() {
          vec2 uv = gl_FragCoord.xy / resolution.xy;
          vec4 posMass = texture2D(texturePosition, uv);
          vec4 vel = texture2D(textureVelocity, uv);
          vec3 p = posMass.xyz;
          vec3 accel = vec3(0.0);

          bool isBH1 = distance(uv, uBH1) < 0.001;
          bool isBH2 = distance(uv, uBH2) < 0.001;

          for(float y = 0.5; y < resolution.y; y += 1.0) {
            for(float x = 0.5; x < resolution.x; x += 1.0) {
              vec2 otherUV = vec2(x, y) / resolution.xy;
              if (otherUV == uv) continue; 

              vec4 otherPosMass = texture2D(texturePosition, otherUV);
              if (otherPosMass.w <= 0.0) continue; 

              vec3 dp = otherPosMass.xyz - p;
              float distSq = dot(dp, dp) + uSofteningSq; 
              float dist = sqrt(distSq);
              accel += ((uG * otherPosMass.w) / (distSq * dist)) * dp;
            }
          }

          vel.xyz += accel * uTimeStep;
          
          if (isBH1 || isBH2) {
            // Heavy drag (uFriction = 0.998) applied ONLY to the black holes
            vel.xyz *= uFriction; 
          } else {
            // "Gas Cooling": Microscopic drag (0.9999) applied to normal stars
            vel.xyz *= 0.9999; 
          }

          vec4 bh1Pos = texture2D(texturePosition, uBH1);
          vec4 bh2Pos = texture2D(texturePosition, uBH2);
          float coreDist = distance(bh1Pos.xyz, bh2Pos.xyz);

          if (coreDist < uMergeRadius) {
              if (isBH1 || isBH2) {
                  vec4 bh1Vel = texture2D(textureVelocity, uBH1);
                  vec4 bh2Vel = texture2D(textureVelocity, uBH2);
                  vel.xyz = (bh1Pos.w * bh1Vel.xyz + bh2Pos.w * bh2Vel.xyz) / (bh1Pos.w + bh2Pos.w);
              }
          }

          float speed = length(vel.xyz);
          if (speed > uMaxSpeed) {
              vel.xyz = (vel.xyz / speed) * uMaxSpeed;
          }

          gl_FragColor = vel;
        }
      `;

      const computePositionShader = `
        uniform float uTimeStep;
        uniform vec2 uBH1;
        uniform vec2 uBH2;
        uniform float uMergeRadius;

        void main() {
          vec2 uv = gl_FragCoord.xy / resolution.xy;
          vec4 posMass = texture2D(texturePosition, uv);
          vec4 vel = texture2D(textureVelocity, uv);
          
          posMass.xyz += vel.xyz * uTimeStep;

          vec4 bh1Pos = texture2D(texturePosition, uBH1);
          vec4 bh2Pos = texture2D(texturePosition, uBH2);
          float coreDist = distance(bh1Pos.xyz, bh2Pos.xyz);

          bool isBH1 = distance(uv, uBH1) < 0.001;
          bool isBH2 = distance(uv, uBH2) < 0.001;

          if (coreDist < uMergeRadius) {
              if (isBH1) {
                  posMass.w = bh1Pos.w + bh2Pos.w; 
                  posMass.xyz = mix(bh1Pos.xyz, bh2Pos.xyz, 0.5); 
              } else if (isBH2) {
                  posMass.w = 0.0; 
                  posMass.xyz = mix(bh1Pos.xyz, bh2Pos.xyz, 0.5); 
              }
          }

          gl_FragColor = posMass; 
        }
      `;

      velocityVariable = gpuCompute.addVariable(
        "textureVelocity",
        computeVelocityShader,
        dtVelocity
      );
      positionVariable = gpuCompute.addVariable(
        "texturePosition",
        computePositionShader,
        dtPosition
      );
      gpuCompute.setVariableDependencies(velocityVariable, [
        positionVariable,
        velocityVariable,
      ]);
      gpuCompute.setVariableDependencies(positionVariable, [
        positionVariable,
        velocityVariable,
      ]);

      velocityVariable.material.uniforms.uTimeStep = { value: TIME_STEP };
      velocityVariable.material.uniforms.uSofteningSq = {
        value: SOFTENING * SOFTENING,
      };
      velocityVariable.material.uniforms.uG = { value: GRAVITY };
      velocityVariable.material.uniforms.uFriction = { value: FRICTION };
      velocityVariable.material.uniforms.uMaxSpeed = { value: MAX_SPEED };
      velocityVariable.material.uniforms.uBH1 = { value: bh1_uv };
      velocityVariable.material.uniforms.uBH2 = { value: bh2_uv };
      velocityVariable.material.uniforms.uMergeRadius = { value: MERGE_RADIUS };

      positionVariable.material.uniforms.uTimeStep = { value: TIME_STEP };
      positionVariable.material.uniforms.uBH1 = { value: bh1_uv };
      positionVariable.material.uniforms.uBH2 = { value: bh2_uv };
      positionVariable.material.uniforms.uMergeRadius = { value: MERGE_RADIUS };

      gpuCompute.init();

      // --- GEOMETRY & MATERIALS ---
      geometry = new THREE.BufferGeometry();
      const renderPositions = new Float32Array(count * 3);
      const references = new Float32Array(count * 2);
      const colors = new Float32Array(count * 3);
      const sizes = new Float32Array(count);

      const g1Cols = GALAXY_1_COLORS.map((c) => new THREE.Color(c));
      const g2Cols = GALAXY_2_COLORS.map((c) => new THREE.Color(c));

      for (let i = 0; i < count; i++) {
        references[i * 2] = (i % width) / width + 0.5 / width;
        references[i * 2 + 1] = Math.floor(i / width) / width + 0.5 / width;

        const isGalaxy1 = i < count / 2;
        const palette = isGalaxy1 ? g1Cols : g2Cols;
        const color = palette[Math.floor(Math.random() * palette.length)]!;

        if (i === bh1_index || i === bh2_index) {
          colors[i * 3 + 0] = 1.0;
          colors[i * 3 + 1] = 1.0;
          colors[i * 3 + 2] = 1.0;
          sizes[i] = BLACK_HOLE_SIZE;
        } else {
          colors[i * 3 + 0] = color.r;
          colors[i * 3 + 1] = color.g;
          colors[i * 3 + 2] = color.b;
          sizes[i] = Math.random() * 5 + 2;
        }
      }

      geometry.setAttribute(
        "position",
        new THREE.BufferAttribute(renderPositions, 3)
      );
      geometry.setAttribute(
        "reference",
        new THREE.BufferAttribute(references, 2)
      );
      geometry.setAttribute("color", new THREE.BufferAttribute(colors, 3));
      geometry.setAttribute("aSize", new THREE.BufferAttribute(sizes, 1));

      material = new THREE.ShaderMaterial({
        depthWrite: false,
        blending: THREE.AdditiveBlending,
        vertexColors: true,
        uniforms: {
          texturePosition: { value: null },
          uPixelRatio: { value: Math.min(window.devicePixelRatio, 2) },
          uBaseSize: { value: PARTICLE_BASE_SIZE },
        },
        vertexShader: `
          uniform sampler2D texturePosition;
          uniform float uPixelRatio;
          uniform float uBaseSize;
          attribute vec2 reference;
          attribute float aSize;
          varying vec3 vColor;
          
          void main() {
            vec4 posData = texture2D(texturePosition, reference);
            
            if (posData.w <= 0.0) {
               gl_Position = vec4(9999.0, 9999.0, 9999.0, 1.0);
               return;
            }

            vec4 viewPosition = viewMatrix * modelMatrix * vec4(posData.xyz, 1.0);
            gl_Position = projectionMatrix * viewPosition;
            gl_PointSize = (aSize * uBaseSize * uPixelRatio) * (1.0 / -viewPosition.z);
            vColor = color;
          }
        `,
        fragmentShader: `
          varying vec3 vColor;
          void main() {
            float dist = distance(gl_PointCoord, vec2(0.5));
            if (dist > 0.5) discard; 
            float alpha = pow(1.0 - (dist * 2.0), 1.2);
            gl_FragColor = vec4(vColor, alpha);
          }
        `,
      });

      points = new THREE.Points(geometry, material);
      scene.add(points);
    };

    // Execute the initial build
    buildGalaxy(currentTextureWidth);

    // ============================================================================
    // 3. ANIMATION & EVENTS
    // ============================================================================
    const fpsInterval = 1000 / TARGET_FPS;
    let then = performance.now();
    let animId = 0;

    const tick = () => {
      const now = performance.now();
      const elapsed = now - then;

      if (elapsed > fpsInterval) {
        then = now - (elapsed % fpsInterval);
        if (window.appStats) window.appStats.begin();

        if (!isPaused && gpuCompute && material) {
          gpuCompute.compute();
          material.uniforms.texturePosition!.value =
            gpuCompute.getCurrentRenderTarget(positionVariable).texture;
        }
        renderer.render(scene, camera);

        if (window.appStats) window.appStats.end();
      }
      animId = window.requestAnimationFrame(tick);
    };
    tick();

    const onResize = () => {
      camera.aspect = window.innerWidth / window.innerHeight;
      camera.updateProjectionMatrix();
      renderer.setSize(window.innerWidth, window.innerHeight);
      renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
      if (material && material.uniforms.uPixelRatio) {
        material.uniforms.uPixelRatio!.value = Math.min(
          window.devicePixelRatio,
          2
        );
      }
    };
    window.addEventListener("resize", onResize);

    const onPause = (e: Event) => {
      isPaused = (e as CustomEvent).detail.paused;
    };
    window.addEventListener("galaxy:pause", onPause);

    // Rebuild the data, but keep the Renderer alive!
    const onGalaxyResize = (e: Event) => {
      currentTextureWidth = (e as CustomEvent).detail.textureWidth;
      buildGalaxy(currentTextureWidth);
    };
    window.addEventListener("galaxy:resize", onGalaxyResize);

    const onGalaxyRadius = (e: Event) => {
      currentGalaxyRadius = (e as CustomEvent).detail.radius;
      buildGalaxy(currentTextureWidth);
    };
    window.addEventListener("galaxy:radius", onGalaxyRadius);

    const onGalaxyOffset = (e: Event) => {
      currentOffset = (e as CustomEvent).detail.offset;
      buildGalaxy(currentTextureWidth);
    };
    window.addEventListener("galaxy:offset", onGalaxyOffset);

    return () => {
      window.removeEventListener("resize", onResize);
      window.removeEventListener("galaxy:pause", onPause);
      window.removeEventListener("galaxy:resize", onGalaxyResize);
      window.removeEventListener("galaxy:radius", onGalaxyRadius);
      window.removeEventListener("galaxy:offset", onGalaxyOffset);
      window.cancelAnimationFrame(animId);
      if (geometry) geometry.dispose();
      if (material) material.dispose();
      renderer.dispose();
      scene.clear();
    };
  };

  let cleanup: (() => void) | undefined;
  document.addEventListener("DOMContentLoaded", () => {
    cleanup = init();
  });
  document.addEventListener("astro:page-load", () => {
    if (cleanup) cleanup();
    cleanup = init();
  });
  document.addEventListener("astro:before-swap", () => {
    if (cleanup) cleanup();
  });
</script>

<style>
  #galaxy-canvas {
    background: transparent;
  }
</style>
